<Window x:Class="BulbPicker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BulbPicker.App"
        xmlns:views="clr-namespace:BulbPicker.App.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Title="Bulb Picker"
        Height="1080" Width="1920">

    <Border CornerRadius="5"
            Background="{StaticResource BackgroundGray}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--Title Bar-->
            <Border Grid.Row="0"
                    Background="{StaticResource HeaderGray}" 
                    CornerRadius="5 5 0 0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <!--Title-->
                    <TextBlock Text="Bulb Picker"
                        Foreground="{StaticResource Yellow}" 
                        VerticalAlignment="Center"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Margin="30 0"/>

                    <!-- Control Buttons -->
                    <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                        <Button Content="✖"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="16"
                        FontWeight="ExtraBold"
                        Margin="0 5 25 0"
                        Cursor="Hand"
                        VerticalAlignment="Center"
                        Foreground="{StaticResource Yellow}"
                        Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!--Content-->
            <Grid Grid.Row="1" Margin="40 30" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="{StaticResource CardGap}"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="{StaticResource CardGap}"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!--Cameras-->
                <Grid Grid.Column="0">
                    <views:CamerasControl />
                </Grid>

                <!--Composite Images & Log-->
                <!-- 화면 작아서 로봇팔 테스트 용으로 일단 없앰 -->
                <!--<Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="{StaticResource CardGap}" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <views:CompositeImagesControl />
                    <views:LogsControl Grid.Row="2" />
                </Grid>-->
                
                <!--Robot Arms & App Config-->
                <!-- 화면 작아서 로봇팔 테스트 용으로 일단 늘림 -->
                <Grid Grid.Column="2" Grid.ColumnSpan="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="{StaticResource CardGap}" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <views:RobotArmsControl />
                    <views:AppConfigControl Grid.Row="2" />
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>
